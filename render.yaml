services:
  - type: web
    name: catalogstream-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn model.api:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
